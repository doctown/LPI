#include <string.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
  char c[10];
  char *cp = c;

  strcpy(cp, strtok(argv[1], "="));
`
  printf("token = %s/t argv = %s/n", c, argv[1]);

  return 0;
}
